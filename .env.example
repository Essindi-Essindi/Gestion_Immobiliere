# Copie ce fichier en ".env" et remplis les valeurs (le vrai .env n'est jamais commite)
# Copy to ".env" and fill in the values (the real .env is never committed)

JWT_SECRET=
DB_URL=jdbc:mysql://localhost:3306/gestion_immobiliere?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
DB_USER=root
DB_PASSWORD=
CORS_ORIGIN=http://localhost:4200
ADMIN_EMAIL=
ADMIN_PASSWORD=
spring.mail.host=
spring.mail.port=587
spring.mail.username=
spring.mail.password=
MAIL_FROM=
